# 2026-06-24

## 项目全面审查
- 通读了所有 18 个源文件 + package.json + README
- 梳理出代码质量优化点：缺测试、stripComments 重复、正则解析局限、无 CI/CD
- 梳理出功能优化方向：环境变量、文件上传、OpenAPI 集成、内置请求发送等

## httpYac 安装提示
- `src/extension.ts`: 添加 `promptHttpYacInstall()` 函数
- `activate()` 中检测 `anweber.vscode-httpyac` 是否安装
- 未安装时弹出通知，可一键安装或永久关闭
- 用 `globalState` 持久化标记，避免重复提示
- README Quick start 更新为推荐 httpYac 作为首选
- 接入 `util/i18n.ts` 的 `t(en, zh)` 模式，中文环境显示中文，默认英文
